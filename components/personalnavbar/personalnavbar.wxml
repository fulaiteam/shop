<view class="navbar custom-class" style='height:{{navHeight}}rpx;background-color:{{bgColor}}'>
  <view wx:if="{{showNav}}" class="navbar-action-wrap navbar-action-group row item-center" style='top:{{navTop}}rpx;'>
      <view class="navbar-action_item" bindtap="_navBack">
        <view class="arrows"></view>
      </view>
  </view>
  <view class='navbar-title' style='top:{{navTop}}rpx'>
    {{pageName}}
  </view>
</view>
